// import {
//   // create naive ui
//   create,
//   // component
//   NButton,
// } from 'naive-ui';
import { type BootModule } from '@/types/boot-module';

// const naive = create({
//   components: [NButton],
// });

export const install: BootModule = app => {
  // app.use(naive);
};
